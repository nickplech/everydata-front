import styled from 'styled-components'

const CloseButton = styled.button`
  background: black;
  color: white;
  font-size: 3rem;
  border: 0;
  position: absolute;
  z-index: 20;
  left: 0;
`

export default CloseButton
